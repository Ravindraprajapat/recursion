class Solution {
    List<List<Integer>> ans = new ArrayList<>();
     public void subset(int[] nums, List<Integer> temp  , int index){
        if(index >= nums.length){
            ans.add(new ArrayList<>(temp));
            return ;
        }
     
        int ele = nums[index];
        temp.add(ele);
        subset(nums , temp , index+1);

        temp.remove(temp.size()-1);
        subset(nums , temp , index+1);

     }
    public List<List<Integer>> subsets(int[] nums) {
        List<Integer> temp = new ArrayList<>();
        int index =0;
        subset(nums , temp , index );
        return ans;
    }
}


📊 FULL COMBINED CALL STACK TREE (EXACT FORMAT)
subset([],0)
├─ subset([1],1)
│  ├─ subset([1,2],2)
│  │  ├─ subset([1,2,3],3)  ✔ return
│  │  └─ subset([1,2],3)    ✔ return
│  └─ subset([1],2)
│     ├─ subset([1,3],3)    ✔ return
│     └─ subset([1],3)      ✔ return
└─ subset([],1)
   ├─ subset([2],2)
   │  ├─ subset([2,3],3)    ✔ return
   │  └─ subset([2],3)      ✔ return
   └─ subset([],2)
      ├─ subset([3],3)      ✔ return
      └─ subset([],3)       ✔ return
